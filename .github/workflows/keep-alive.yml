name: Keep Render Alive
# ⛔ DISABLED — ngrok 로컬 배포로 전환 (2026-03-20)
# Render 프리티어 슬립 방지 ping은 더 이상 필요 없음

on:
  workflow_dispatch:   # 수동 실행만 허용 (스케줄 제거)

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Disabled
        run: echo "keep-alive disabled — using ngrok local deploy"
