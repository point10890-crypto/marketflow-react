name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # 운영 최저 버전(3.11)과 맞춘다 — 3.12+ 전용 문법이 섞이는 사고를 CI 에서 잡는다
          python-version: '3.11'
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt
      - name: Run pytest (full suite)
        env:
          PYTHONIOENCODING: utf-8
        run: python -m pytest tests/ -q

  frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: frontend-react
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend-react/package-lock.json
      - run: npm ci
      # lint 는 package.json 에 --max-warnings 0 으로 정의돼 있었지만 CI 에서 실행되지 않았다
      - run: npm run lint
      - run: npm test
      # 타입 오류·프리렌더 실패가 배포 시점에야 드러나지 않게 빌드까지 검증
      - run: npm run build
