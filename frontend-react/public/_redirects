# Keep the Google verification file available at its exact .html URL.
/googled3865368a0313948.html /googled3865368a0313948 200

# Proxy only known React routes. Pages canonicalizes /index.html proxy targets
# to a 308 redirect, so use the equivalent root index target instead.
# Deliberately do not add a catch-all: missing /assets/* requests must stay 404.
# Public React routes
/ / 200
/login / 200
/signup / 200
/plan-select / 200
/payment-request / 200
/pending-approval / 200

# Public content zone (AdSense-reviewable)
# /community /pricing /privacy /terms /about 은 scripts/prerender-seo.mjs 가
# dist/<경로>/index.html 정적 스냅샷을 생성하므로 프록시 라인을 두지 않는다
# (Cloudflare Pages 는 리다이렉트를 정적 자산보다 먼저 평가 — 라인이 있으면 스냅샷이 가려진다).
# 게시글·게시판 등 동적 하위 경로만 SPA 로 프록시.
/community/* / 200

# Authenticated React route trees
/dashboard / 200
/dashboard/* / 200
/admin / 200
/admin/* / 200
