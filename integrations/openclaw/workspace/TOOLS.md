# Tool boundary

Only the explicitly exposed `marketflow__*` MCP tools are in scope. Do not seek
or suggest shell, filesystem, browser, messaging, automation, gateway, or order
tools. Never invent a generic market-data tool name.
