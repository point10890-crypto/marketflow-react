# MarketFlow — 런타임 의존성 (핀 버전). 원본 명세는 requirements.in, 테스트 의존성은 requirements-dev.txt.
# Python 3.11+ — 검증 환경: Python 3.11 / Linux CI (2026-09-03)
#
# 갱신 절차: requirements.in 수정 → 깨끗한 venv 에 설치 → pip freeze 에서 아래 패키지만 옮겨 적기.
# 리뷰(2026-09-02 §3.4-6): 전부 `>=`/무제약이라 miniPC·CI·본PC 가 서로 다른 버전을 돌렸다.

# Flask 서버
flask==3.1.3
flask-compress==1.24
flask-cors==6.0.5
flask-sqlalchemy==3.1.1
bcrypt==5.0.0

# 데이터 수집
pykrx==1.2.8
requests==2.34.2
beautifulsoup4==4.15.0
finance-datareader==0.9.202
finnhub-python==2.4.29
fredapi==0.5.2
selenium==4.48.0
webdriver-manager==4.1.2

# 데이터 처리
pandas==2.3.3
numpy==2.4.6
yfinance==1.7.0
scipy==1.17.1
scikit-learn==1.9.0

# 시각화 (portfolio_risk, backtest)
matplotlib==3.11.1
seaborn==0.13.2

# 스케줄링
schedule==1.2.2

# AI/LLM
google-generativeai==0.8.6
google-genai==2.22.0
openai==3.7.0
anthropic==1.3.0
mcp==1.29.1

# 기타
python-dotenv==1.2.3
tqdm==4.70.0
openpyxl==3.1.5

# 결제
stripe==15.6.1

# 배포/운영
gunicorn==26.2.0
aiohttp==3.14.3
setuptools==79.0.1
certifi==2026.7.22
filelock==3.32.5
holidays==0.103
